
# Think about what Inputs , Ouputs  and Methods will be used:

class WetherGetter:
    pass


class MongoHandler:
    pass





'''attempted an SQL QUERY WIZARD Code not working :( Cannot find SQLITE3 '''
# class basic(object):
#       import sqlite3
#     def __init__(self):
#         self.conn = sqlite3.connect('database.sqlite')
#         self.cur = sqlite3.connect('database.sqlite').cursor()
    

# class SQL_wizard:
#     def __init__(self, conn):
#         self.conn = sqlite3.connect(conn)
#         self.cur = sqlite3.connect(conn).cursor()
            

    
    
#     def query(self, SELECT,
#               FROM,
#               WHERE=None,
#               GROUPBY=None,
#               HAVING=None,                     
#               LIMIT=None):
           
#         query = (''' SELECT {} 
#                      FROM {} 
#                      WHERE {}
#                      GROUPBY {}
#                      HAVING {}
#                      LIMIT {}'''.format(SELECT,
#                                         FROM,
#                                         WHERE,
#                                         GROUPBY,
#                                         HAVING,
#                                         LIMIT))
#         return pd.read_sql_query(query, self.conn)
        
        

# del_db = SQL_wizard('database.sqlite')



    